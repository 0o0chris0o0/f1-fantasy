[build]
  base = "/"
  command = "pnpm --filter web build"
  publish = "apps/web/.output/public"

[build.environment]
  # Crucial for Nuxt + pnpm on Netlify
  PNPM_FLAGS = "--shamefully-hoist"
  # Set the Node version to match your local environment
  NODE_VERSION = "20" 